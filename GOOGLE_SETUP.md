# إعداد الدخول بحساب قوقل

زر قوقل موجود في اللعبة، لكنه **ما راح يشتغل** حتى تكمل الخطوات هنا.
كلها في لوحات تحكم خارجية — ما أحد يقدر يسويها غيرك لأنها مربوطة بحسابك.

الوقت المتوقع: ١٠–١٥ دقيقة.

---

## قبل ما تبدأ — جهّز معلومتين

| | القيمة |
|---|---|
| رابط مشروع Supabase | `https://jydohcccucwwnxgbdyqu.supabase.co` |
| رابط موقعك المنشور | تلقاه في لوحة الاستضافة (مثلاً `https://xxxx.netlify.app`) |

كل مكان مكتوب فيه `<رابط موقعك>` بالأسفل، حط رابط موقعك الفعلي مكانه.

---

## الجزء الأول: Google Cloud Console

### ١. أنشئ مشروعاً

افتح [console.cloud.google.com](https://console.cloud.google.com) وسجّل دخول.

من أعلى الصفحة اضغط قائمة المشاريع ← **New Project** ← سمّه `Khamen Sah` ← **Create**.

انتظر لين يتحوّل المشروع النشط للمشروع الجديد (يظهر اسمه فوق).

### ٢. اضبط شاشة الموافقة

من القائمة الجانبية: **APIs & Services** ← **OAuth consent screen**

- **User Type**: اختر **External** ← **Create**
- **App name**: `خمّن صح`
- **User support email**: بريدك
- **Developer contact information**: بريدك
- **Save and Continue**

في صفحة **Scopes** اضغط **Save and Continue** بدون تغيير.
في صفحة **Test users** اضغط **Save and Continue**.

> ⚠️ **أهم نقطة في الدليل كله:** المشروع الآن في وضع **Testing**، ومعناه إن
> **أحد ما يقدر يدخل إلا الحسابات اللي تضيفها يدوياً كـ Test users**.
> عشان يفتح لكل الناس، ارجع لـ **OAuth consent screen** واضغط
> **PUBLISH APP** ← **Confirm**.
>
> لو نسيت هذي الخطوة، كل لاعب غيرك بيشوف رسالة
> "Access blocked: خمّن صح has not completed the Google verification process"
> وتظن إن الكود خربان — وهو سليم.

### ٣. أنشئ بيانات الاعتماد

**APIs & Services** ← **Credentials** ← **+ CREATE CREDENTIALS** ← **OAuth client ID**

- **Application type**: `Web application`
- **Name**: `Khamen Sah Web`

**Authorized JavaScript origins** — اضغط **+ ADD URI** وأضف:

```
<رابط موقعك>
```

**Authorized redirect URIs** — اضغط **+ ADD URI** وأضف **هذا بالضبط**:

```
https://jydohcccucwwnxgbdyqu.supabase.co/auth/v1/callback
```

> هذا رابط Supabase مو رابط موقعك. قوقل يرجّع اللاعب لـ Supabase أولاً،
> وSupabase يرجّعه لموقعك. لو حطيت رابط موقعك هنا، الدخول يفشل برسالة
> `redirect_uri_mismatch`.

اضغط **CREATE**.

راح تطلع لك نافذة فيها **Client ID** و **Client Secret** — **انسخهما الآن**،
الـ Secret ما يظهر كامل مرة ثانية.

---

## الجزء الثاني: Supabase

### ٤. فعّل مزوّد قوقل

افتح [supabase.com/dashboard](https://supabase.com/dashboard) ← مشروعك ←
**Authentication** ← **Providers** ← دوّر على **Google** واضغطه.

- فعّل **Enable Sign in with Google**
- **Client ID**: الصق اللي نسخته
- **Client Secret**: الصق اللي نسخته
- **Save**

### ٥. اضبط روابط الرجوع

**Authentication** ← **URL Configuration**

- **Site URL**: `<رابط موقعك>`
- **Redirect URLs** ← **Add URL**:

```
<رابط موقعك>
<رابط موقعك>/index.html
```

**Save**

> لو ما أضفت الموقع هنا، اللاعب يسجّل دخوله في قوقل بنجاح ثم يُرجَع
> لصفحة خطأ بدل ما يرجع للعبة.

---

## الجزء الثالث: التجربة

### ٦. جرّب بنفسك

١. افتح موقعك في **نافذة تصفح خفي** (عشان ما تتداخل مع جلستك الحالية)
٢. في شاشة الدخول اضغط **الدخول بحساب قوقل**
٣. اختر حسابك ووافق
٤. المفروض ترجع للعبة وتطلع لك نافذة **"اختر اسمك"**
٥. اكتب اسم مستخدم ← **متابعة**
٦. المفروض تدخل اللعبة برسالة "تم إنشاء حسابك عبر قوقل"

### ٧. تأكد من قاعدة البيانات

Supabase ← **Table Editor** ← `profiles`

لازم تلقى صفاً جديداً فيه:

| العمود | القيمة المتوقعة |
|---|---|
| `username` | اللي اخترته |
| `email` | بريد قوقل |
| `auth_id` | معرّف طويل (UUID) — **مو فاضي** |
| `password_hash` | `!google-only` |

`!google-only` قيمة مقصودة: `crypt()` ما ينتج هذا النص أبداً، فما فيه كلمة
مرور تطابقها. يعني الحساب يدخل بقوقل فقط حتى يضبط صاحبه كلمة مرور.

---

## أخطاء شائعة

| الرسالة | السبب | الحل |
|---|---|---|
| `redirect_uri_mismatch` | رابط الرجوع في قوقل غلط | تأكد إنه رابط **Supabase** لا موقعك (خطوة ٣) |
| `Access blocked ... verification process` | التطبيق لسه في وضع Testing | اضغط **PUBLISH APP** (خطوة ٢) |
| يرجع لصفحة خطأ بعد الموافقة | الموقع مو في Redirect URLs | أضفه في Supabase (خطوة ٥) |
| `Unsupported provider` | المزوّد مو مفعّل | فعّله في Supabase (خطوة ٤) |
| يدخل ثم يخرج فوراً | ملفات الموقع قديمة في المتصفح | `Ctrl+Shift+R` |

---

## ملاحظات

**اللاعبون القدامى:** حساباتهم بلا بريد، فربط قوقل ما يقدر يطابقهم. أول ما
يسجّل أحدهم دخوله راح تطلع له نافذة "أضف بريدك". بعد ما يضيفه، لو دخل بقوقل
بنفس البريد **يُربط تلقائياً** بحسابه الموجود ولا يُنشأ حساب مكرر.

**اختلاف البريد:** لو بريد قوقل عند اللاعب غير البريد المسجّل في حسابه،
الربط التلقائي ما يشتغل. فيه دالة `link_google_to_account` جاهزة في قاعدة
البيانات لهذي الحالة، لكن ما بنيت لها واجهة — قول لي لو احتجتها.

**الأمان:** دوال الربط تقرأ هوية اللاعب من `auth.uid()` داخل قاعدة البيانات،
مو من قيمة يرسلها المتصفح. لو كانت تعتمد على قيمة مرسلة، أي أحد يقدر يرسل
معرّف لاعب ثاني ويستولي على حسابه.
